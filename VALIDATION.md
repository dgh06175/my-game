# v1 검증 기록

검사일: 2026-09-06. 아래 자동 검사는 저장소의 테스트로 다시 실행할 수 있습니다.

## 게임 규칙과 저장

`npm test`: 30개 통과.

- 해역마다 100개, 총 300개 생성 시드에서 필수 목표·귀환 경로와 산소 여유를 검사했습니다.
- 스캔·채집·작살 사냥·절단, 사냥 재료를 사용하는 장비 성장과 최종 발견을 검사했습니다.
- 사망 및 새로고침으로 현재 탐사만 실패하고 기존 장비·재료·도감·목표 달성이 남는지 검사했습니다.
- 귀환 보상과 탐사 종료의 원자적 저장, 같은 탐사의 중복 지급 방지와 여러 저장 연결의 동시 종료를 검사했습니다.
- 도감 저장이 일시적으로 실패한 뒤 귀환해도 발견 기록이 유지되는지 검사했습니다.
- 기지 겹침·방 밖 배치 차단, 저장 복원, 파일 내보내기/가져오기 왕복, 잘못된 버전·항목·잠긴 장식 입력 거부를 검사했습니다.

## 브라우저 조작

`npm run test:e2e`: 로컬 Chrome에서 5개 시나리오 통과.

| 화면 | 확인한 행동 |
| --- | --- |
| 데스크톱 1440 × 900 | 모든 기지 메뉴, 배치 및 복원, 저장 파일 왕복과 잘못된 파일 처리 |
| 데스크톱 1440 × 900 | 마우스로 스캔·채집·사냥, 재료 회수·귀환, 수족관 전시 |
| 데스크톱 1440 × 900 | 일시정지, 새로고침 시 탐사 실패와 영구 기록 유지 |
| 데스크톱 1440 × 900 | 내압복·절단기 제작, 난파선 절단, 심해 장비 제작, 빛결가오리 스캔과 목표 유지 |
| 모바일 844 × 390 | 터치 패드 이동·조준·도구 사용·사냥·귀환·제작·기지 배치·수족관 전시, 세로 전환 시 일시정지 |

브라우저 검사는 이동 거리와 재료 반복 수집을 줄이는 개발 전용 준비 데이터를 사용하며, 실제 입력과 게임 규칙으로 상호작용을 수행합니다. 이 접근자는 배포 빌드에 포함되지 않습니다.

## 빌드와 표현

- `npm run build`: TypeScript 검사와 Vite 배포 빌드 통과.
- 배포용 빌드를 별도로 열어 기지, 탐사 시작과 새로고침 후 기지 복귀를 확인했습니다.
- 산호초·심해, 기지·도감·장비 화면과 모바일 가로 화면의 캡처를 시각 검토했습니다.
- 3D 모델·텍스처 효과와 소리는 코드에서 생성합니다. 글꼴과 라이선스 파일은 배포물에 포함합니다.

## 측정의 범위

자동 브라우저 검사는 소프트웨어 WebGL과 저품질 설정으로 수행했습니다. GitHub의 CPU 렌더러에서는 화면 배치·입력 좌표를 유지하면서 렌더 픽셀 비율을 0.5로 낮춥니다. 로컬 검사는 픽셀 비율 1로 수행했습니다. 모바일 항목은 터치 입력 및 화면 크기 재현 검사이며 실제 휴대전화에서 수행한 검사가 아닙니다. 실제 PC 1080p 60fps와 모바일 30fps 달성 여부는 아직 기기별로 측정하지 않았습니다.

탐사 5–10분과 첫 목표 30–60분은 초기 게임 수치의 설계 기준입니다. 위 준비 데이터를 사용하는 자동 진행 검사를 사람의 실제 완주 시간 측정으로 간주하지 않습니다.
